package com.learn.Arrays;

import java.util.Arrays;

public class RemoveDuplicates {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*
		 * int[] nums= {0,0,1,1,1,2,2,3,3,4};
		 * System.out.println(Arrays.toString(removeDupicates(nums)));
		 */
	}
	
	/*
	 * public static int[] removeDupicates(int[] nums) { int max = nums[nums.length
	 * - 1] + 10; for (int i = 1; i < nums.length; i++) { int val = nums[i]; if
	 * (nums[i] == val) { nums[i] = max; } else { val = nums[i]; } }
	 * Arrays.sort(nums); int size = 0; int j = 0; int[] arr = new int[nums.length -
	 * 1]; for (int num : nums) { if (num != max) { size++; arr[j] = num; j++; } }
	 * return arr; }
	 */

}
